#include "../includes/NickCommand.hpp"

void NickCommand::execute(const std::vector<std::string>& args, int socket, Server& server) {
    // Perform the nick logic here
    // For example, change the nickname of a user in globalState
    (void) args;
    (void) socket;
    (void) server;
}
